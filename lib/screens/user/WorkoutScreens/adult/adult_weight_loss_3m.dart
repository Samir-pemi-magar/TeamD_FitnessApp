import 'package:flutter/material.dart';

class AdultWeightLoss3MonthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Adult - 3 Month Weight Loss")),
      body: Center(child: Text("Adult Workout - 3 Month Weight Loss")),
    );
  }
}
