import 'package:flutter/material.dart';

class AdultMuscleBuildingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Adult - Muscle Building")),
      body: Center(child: Text("Adult Workout - Muscle Building")),
    );
  }
}
