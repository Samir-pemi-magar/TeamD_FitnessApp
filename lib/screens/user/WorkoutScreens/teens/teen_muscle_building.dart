import 'package:flutter/material.dart';

class TeenMuscleBuildingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Teen - Muscle Building")),
      body: Center(child: Text("Teen Workout - Muscle Building")),
    );
  }
}
