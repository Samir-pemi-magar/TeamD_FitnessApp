import 'package:flutter/material.dart';

class SeniorMuscleBuildingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Senior - Muscle Building")),
      body: Center(child: Text("Senior Workout - Muscle Building")),
    );
  }
}
