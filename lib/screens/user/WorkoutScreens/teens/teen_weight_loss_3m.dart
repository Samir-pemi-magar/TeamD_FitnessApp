import 'package:flutter/material.dart';

class TeenWeightLoss3MonthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Teen - 3 Month Weight Loss")),
      body: Center(child: Text("Teen Workout - 3 Month Weight Loss")),
    );
  }
}
