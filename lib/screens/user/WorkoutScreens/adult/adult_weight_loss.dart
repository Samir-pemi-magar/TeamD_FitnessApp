import 'package:flutter/material.dart';

class AdultWeightLossScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Adult - Weight Loss")),
      body: Center(child: Text("Adult Workout - Weight Loss")),
    );
  }
}
