import 'package:flutter/material.dart';

class TeenWeightLossScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Teen - Weight Loss")),
      body: Center(child: Text("Teen Workout - Weight Loss")),
    );
  }
}
